# Local Modifications

This directory contains a project-local copy of `guizang-social-card-skill`, which remains licensed under the included `LICENSE` (GNU AGPL-3.0-or-later).

## 2026-08-22

- Renamed the containing project skill directory from `card` to `guizang-social-card-skill` so it matches the skill frontmatter name.
- Replaced the legacy `raw/content/articles/<article-slug>/` output convention in runtime skill instructions with `content/projects/<content-slug>/`.

These changes apply only to the copied skill. It remains a separate skill package and must not be merged into dabaihua-studio application source files without a separate license review.
